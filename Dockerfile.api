FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y \
    gcc g++ libpq-dev poppler-utils tesseract-ocr \
    libgl1 libglib2.0-0 libsm6 libxext6 curl \
    && rm -rf /var/lib/apt/lists/*

# Install PyTorch CPU-only FIRST to prevent docling pulling 7GB of CUDA
RUN pip install --no-cache-dir \
    torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu

# Now install remaining dependencies
COPY requirements.api.txt .
RUN pip install --no-cache-dir -r requirements.api.txt

COPY . .
RUN mkdir -p /app/data /app/prompts

EXPOSE 8000
CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
